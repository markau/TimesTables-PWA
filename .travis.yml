language: node_js

node_js:
  - node

before_script:
  - npm install -g --silent @angular/cli

script:
  - ng build --prod

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email:
    on_failure: change
    on_success: change
