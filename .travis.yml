language: node_js

node_js:
  - node

before_script:
  - npm install -g --silent @angular/cli

script:
  - ng build --prod
  - npm install codecov -g

after_success:
  - codecov

notifications:
  email:
    on_failure: change
    on_success: change
